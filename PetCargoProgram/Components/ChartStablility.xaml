<UserControl x:Class="PetCargoProgram.Components.ChartStablility"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:PetCargoProgram.Components"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300"
             x:Name="Component">
    <UserControl.Resources>
        <ResourceDictionary Source="Styles/Styles.xaml"/>
    </UserControl.Resources>
<!-- <Grid DataContext="{Binding ElementName=Component}"> -->
    <Grid Margin="20"
          DataContext="{Binding ElementName=Component}"
          x:Name="GridMain">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <!-- <Rectangle  VerticalAlignment="Stretch" Grid.Row="1"  HorizontalAlignment="Stretch"> -->
        <!--     <Rectangle.Fill> -->
        <!--         <LinearGradientBrush StartPoint="0,0" EndPoint="0,1"> -->
        <!--             <GradientStop Color="DodgerBlue" Offset="0"/> -->
        <!--             <GradientStop Color="Transparent" Offset="1"/> -->
        <!--         </LinearGradientBrush> -->
        <!--     </Rectangle.Fill> -->
        <!-- </Rectangle> -->
        <Image Name="image1" VerticalAlignment="Bottom" HorizontalAlignment="Stretch"
               Grid.Row="0">
            <!-- <Image.RenderTransform> -->
            <!--     <TransformGroup> -->
            <!--         <RotateTransform Angle="{Binding Angle, UpdateSourceTrigger=PropertyChanged, Mode=OneWay}" -->
            <!--                          CenterY="{Binding Draft, UpdateSourceTrigger=PropertyChanged, Mode=OneWay}" -->
            <!--                          CenterX="{Binding LCF, UpdateSourceTrigger=PropertyChanged, Mode=OneWay}"/> -->
            <!--         <TranslateTransform Y="{Binding Draft, UpdateSourceTrigger=PropertyChanged, Mode=OneWay}"/> -->
            <!--     </TransformGroup> -->
            <!-- </Image.RenderTransform> -->
        </Image>
        <!--     <Image.RenderTransform> -->
        <!--         <TransformGroup> -->
        <!--             <RotateTransform Angle="{Binding Angle, UpdateSourceTrigger=PropertyChanged, Mode=OneWay}" -->
        <!--                              CenterY="{Binding Draft, UpdateSourceTrigger=PropertyChanged, Mode=OneWay}" -->
        <!--                              CenterX="120.0"/> -->
        <!--             <TranslateTransform> -->
        <!--                 <TranslateTransform.Y> -->
        <!--                     <MultiBinding -->
        <!--                         UpdateSourceTrigger="PropertyChanged" -->
        <!--                         Converter="{StaticResource Converter_Draft}"> -->
        <!--                         <Binding Path="Draft" UpdateSourceTrigger="PropertyChanged" Mode="OneWay"/> -->
        <!--                         <TemplateBinding Property="Height" Path="GridMain.Height" UpdateSourceTrigger="PropertyChanged" Mode="OneWay"/> -->
        <!--                     </MultiBinding> -->
        <!--                 </TranslateTransform.Y> -->
        <!--             </TranslateTransform> -->
        <!--         </TransformGroup> -->
        <!--     </Image.RenderTransform> -->
        <!-- </Image> -->
    </Grid>
    <!-- <StackPanel Orientation="Vertical"> -->
    <!--     <Label Content="{Binding Path=Draft, StringFormat=Draft \{0:f2\}}"/> -->
    <!--     <Label Content="{Binding Path=LCF, StringFormat=LCF \{0:f2\}}"/> -->
    <!--     <Label Content="{Binding Path=Angle, StringFormat=Angle \{0:f2\}}"/> -->
    <!-- </StackPanel> -->
<!-- </Grid> -->


</UserControl>

